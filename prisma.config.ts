// prisma.config.ts — Prisma 5 no requiere este archivo.
// La URL de conexión se lee de la variable de entorno DATABASE_URL
// configurada en .env.local (dev) y en Vercel (producción).
// Este archivo se mantiene por compatibilidad con herramientas del workspace.
